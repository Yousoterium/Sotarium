import { createServer } from "vite";
import { writeFileSync } from "node:fs";

const vite = await createServer({
  root: process.cwd(),
  server: { middlewareMode: true },
  appType: "custom",
  logLevel: "error",
});

try {
  const { generateFullKeySystemScript } = await vite.ssrLoadModule("/src/lib/scriptGenerator.ts");
  const output = generateFullKeySystemScript(
    [
      {
        id: "game-1",
        name: "San Diego Border Roleplay",
        imageUrl: "https://raw.githubusercontent.com/Yousoterium/Sotarium/main/images/game1.png",
        placeId: "136020512003847",
        scriptUrl: "https://example.com/san-diego.lua",
      },
      {
        id: "game-2",
        name: "Second Game",
        imageUrl: "https://example.com/second-game.png",
        placeId: "987654321",
        scriptUrl: "https://example.com/second-game.lua",
      },
    ],
    undefined,
    ""
  );

  writeFileSync("/tmp/sotarium-generated-runtime.lua", output);
  const required = [
    'local ContentFrame = Instance.new("Frame")',
    'local SubmitButton = Instance.new("TextButton")',
    'local GetKeyButton = Instance.new("TextButton")',
    'local SupportedGamesButton = Instance.new("TextButton")',
    'local JoinDiscordButton = Instance.new("TextButton")',
    'local function clearExistingSotariumGui()',
    'ScreenGui.DisplayOrder = 2147483647',
    'Generated by /add: complete home GUI build 2026.08.26.2',
    'GamesOverlay.Visible = false',
    'Second Game',
  ];
  const missing = required.filter((snippet) => !output.includes(snippet));
  const report = {
    characters: output.length,
    lines: output.split("\n").length,
    firstScreenIsHome: output.indexOf('local ContentFrame = Instance.new("Frame")') < output.indexOf('local GamesOverlay = Instance.new("Frame")'),
    startsWithGamesOverlayVisible: output.includes("GamesOverlay.Visible = true\n\n--"),
    missing,
  };
  console.log(JSON.stringify(report, null, 2));
  if (report.characters < 1500 || !report.firstScreenIsHome || report.startsWithGamesOverlayVisible || missing.length > 0) {
    process.exitCode = 1;
  }
} finally {
  await vite.close();
}
